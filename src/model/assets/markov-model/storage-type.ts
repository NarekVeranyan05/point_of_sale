/**
 * StorageType is an enumeration for type of storage (i.e. container class) that
 * a {@link Coupon} or ${@link Product} can have. This enumeration is used for
 * storing and fetching for those classes.
 */
export enum StorageType { CART = "CART", RECEIPT = "RECEIPT" }